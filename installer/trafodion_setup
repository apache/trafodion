#!/bin/bash

# @@@ START COPYRIGHT @@@
#
# (C) Copyright 2013-2014 Hewlett-Packard Development Company, L.P.
#
#  Licensed under the Apache License, Version 2.0 (the "License");
#  you may not use this file except in compliance with the License.
#  You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS,
#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#  See the License for the specific language governing permissions and
#  limitations under the License.
#
# @@@ END COPYRIGHT @@@

#  Setup Trafodion environment on a Cluster System

#==============================================
# Default values
typeset TRAF_WORKDIR="$( cd "$( dirname "$0" )" && pwd )/.."
typeset TRAF_USER="trafodion"
typeset TRAF_USER_PASSWORD="traf123"   # 8 char max
typeset NODE_LIST=""
typeset CLUSTERNAME="$HOSTNAME"
typeset HOME_DIR="/home"
typeset timestamp=$(date +%F-%H-%M-%S)
typeset SETUP_LOG="/var/log/trafodion/trafodion_setup_$timestamp.log"
sudo mkdir -p /var/log/trafodion
sudo chmod 777 /var/log/trafodion
#==============================================

function print_usage {
cat << EOF
This script will do the one-time setup needed
to establish the Trafodion environment.

NOTE: This script does NOT install a Trafodion build
      it merely sets up everything needed to support
      Trafodion.

Usage: $(basename $0) [options]

Options:
    --help                 Print this message and exit.
    --nodes "<node_list>"  Required blank separated list of nodes (within quotes) in this Trafodion environment
                           From 1 to N nodes, although two is the recommended minimum.
    --username <user>      Trafodion user name (default "$TRAF_USER")
    --password <password>  Password for the Trafodion user name (default "$TRAF_USER_PASSWORD")
    --clustername <name>   Name of Trafodion cluster. (default $HOSTNAME)
    --home_dir <path>      Path to Trafodion home directory. (default /home)
EOF
}

#==============================================
# Parse input parameters
while [[ $# -gt 0 ]]; do
    case "$1" in
        --username)
            if [[ -z "$2" ]]; then
                echo "***ERROR: No value passed to param $1."
                print_usage
                exit -1
            fi
            TRAF_USER=$2
            shift
            ;;
        --password)
            if [[ -z "$2" ]]; then
                echo "***ERROR: No value passed to param $1."
                print_usage
                exit -1
            fi
            TRAF_USER_PASSWORD=$2
            shift
            ;;
        --nodes)
            if [[ -z "$2" ]]; then
                echo "***ERROR: No value passed to param $1."
                print_usage
                exit -1
            fi
            NODE_LIST=$2
            shift
            ;;
        --clustername)
            if [[ -z "$2" ]]; then
                echo "***ERROR: No value passed to param $1."
                print_usage
                exit -1
            fi
            CLUSTERNAME=$2
            shift
            ;;
        --home_dir)
            if [[ -z "$2" ]]; then
               echo "***ERROR: No value passed to param $1."
               print_usage
               exit -1
            fi
            HOME_DIR=$2
            shift
            ;;
        --help)
            print_usage
            exit -1
            ;;
        *)
            echo "***ERROR: unknown parameter '$1'"
            print_usage
            exit -1
    esac
    shift
done

if [[ $NODE_LIST == "" ]]; then
   echo 
   echo "***ERROR: Please use --nodes option to include node list" 
   echo "***ERROR: Exiting..."
   echo
   exit -1 
fi

#==============================================
echo
echo "***Starting Trafodion Setup***"
echo "***Installer Version 0.9.0_v3***" | tee -a $SETUP_LOG
echo
#Info for log file
echo "***INFO: Log file located at /var/log/trafodion"
echo
echo "***Log File: trafodion_setup script***" >> $SETUP_LOG
echo >> $SETUP_LOG
echo "Username: $TRAF_USER" >> $SETUP_LOG
echo "Nodes: $NODE_LIST" >> $SETUP_LOG
echo "Clustername $CLUSTERNAME" >> $SETUP_LOG
echo "Home directory: $HOME_DIR" >> $SETUP_LOG
echo >> $SETUP_LOG

#=============================================
#Run setup script 

$TRAF_WORKDIR/installer/traf_setup \
    --username $TRAF_USER \
    --password $TRAF_USER_PASSWORD \
    --nodes "$NODE_LIST" \
    --clustername "$CLUSTERNAME" \
    --home_dir "$HOME_DIR" | tee -a $SETUP_LOG

if [ ${PIPESTATUS[0]} != "0" ]; then
   echo "***ERROR: Error while running trafodion_setup." | tee -a $SETUP_LOG
   echo "***ERROR: Setup not complete, review logs." | tee -a $SETUP_LOG
   echo "***ERROR: Exiting...." | tee -a $SETUP_LOG
   exit -1 
fi

echo "***INFO: Trafodion setup completed" | tee -a $SETUP_LOG

