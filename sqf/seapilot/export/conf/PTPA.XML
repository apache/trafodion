<?xml version="1.0" encoding="UTF-8"?>
<publicationConfiguration>

  <!-- Description: SQL error events                                       -->
  <!-- Table: instance_repository.event_sqlerror_table                     -->
  <!-- Publication: sql.error_event                                        -->
  <!-- =================================================================== -->
  <publicationMap>
    <!-- ************************************************ -->
    <!-- Define the publication name and repository table -->
    <!-- ************************************************ -->
    <amqpRoutingKey>event.sql.instance.public.gpb.error_event</amqpRoutingKey>
    <repositoryTableName>manageability.instance_repository.event_sqlerror_table</repositoryTableName>
  </publicationMap>

  <!-- =================================================================== -->
  <!-- Description: SQL informational events                               -->
  <!-- Table: instance_repository.event_sqlinfo_table                      -->
  <!-- Publication: sql.info_event                                         -->
  <!-- =================================================================== -->
  <publicationMap>
    <!-- ************************************************ -->
    <!-- Define the publication name and repository table -->
    <!-- ************************************************ -->
    <amqpRoutingKey>event.sql.instance.public.gpb.info_event</amqpRoutingKey>
    <repositoryTableName>manageability.instance_repository.event_sqlinfo_table</repositoryTableName>
  </publicationMap>

  <!-- =================================================================== -->
  <!-- Description: DTM events                                             -->
  <!-- Table: instance_repository.event_tm_table                           -->
  <!-- Publication: dtm.events                                             -->
  <!-- =================================================================== -->
  <publicationMap>
    <!-- ************************************************ -->
    <!-- Define the publication name and repository table -->
    <!-- ************************************************ -->
    <amqpRoutingKey>event.dtm.instance.public.gpb.events</amqpRoutingKey>
    <repositoryTableName>manageability.instance_repository.event_tm_table</repositoryTableName>
  </publicationMap>

  <!-- =================================================================== -->
  <!-- Description: chameleon events                                       -->
  <!-- Table: instance_repository.event_seapilot_table                     -->
  <!-- Publication: chameleon.events                                       -->
  <!-- =================================================================== -->
  <publicationMap>
    <!-- ***************************************************************** -->
    <!-- Define the publication name and repository table.                 -->
    <!-- ***************************************************************** -->
    <amqpRoutingKey>event.chameleon.instance.public.gpb.events</amqpRoutingKey>
    <repositoryTableName>manageability.instance_repository.event_seapilot_table</repositoryTableName>
  </publicationMap>

</publicationConfiguration>
